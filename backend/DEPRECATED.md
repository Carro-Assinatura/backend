# Backend Express - DEPRECATED

Este backend foi substituído pelo **Supabase** (PostgreSQL + Auth nativo).

O frontend agora se conecta diretamente ao Supabase usando `@supabase/supabase-js`.

Este diretório é mantido como backup. Pode ser removido com segurança.
